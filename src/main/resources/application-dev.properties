# Development Environment Configuration

# Database - Local
spring.datasource.url=jdbc:mysql://localhost:3306/personnel_tracking?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=sahinbey_
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Database - External
external.datasource.url=jdbc:mysql://193.140.136.145:3306/isicil?useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
external.datasource.username=${EXTERNAL_DB_USERNAME:hesapi}
external.datasource.password=${EXTERNAL_DB_PASSWORD:TuruncuManTiS2025!!}
external.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
external.datasource.hikari.connection-test-query=SELECT 1
external.datasource.hikari.leak-detection-threshold=60000

# External JPA/Hibernate Configuration
external.jpa.hibernate.ddl-auto=none
external.datasource.hikari.auto-commit=true
external.datasource.hikari.read-only=true

# JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# JWT (use weak secret for dev only - NEVER use in production)
jwt.secret=dev-secret-key-not-for-production-use-only
jwt.expiration=1800000
jwt.issuer=personnel-tracking-system-dev

# SMS Gateway - Mock SMS for testing (shows OTP in console)
mock.sms.enabled=true
sms.gateway.url=http://localhost:8080/mock-sms
sms.gateway.api-id=29d463733f56db81be9eb355
sms.gateway.api-key=79259ea325e14e8603ab7cf7
sms.gateway.sender=G.ANTEP UNI
otp.length=6

# Logging
logging.level.com.bidb.personetakip=DEBUG
logging.level.org.springframework.security=DEBUG

# Disable SSL in development
server.ssl.enabled=false

# CORS - Allow all origins in development
cors.allowed-origins=*
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS,PATCH
cors.allow-credentials=true

# Thymeleaf hot reload
spring.thymeleaf.cache=false
