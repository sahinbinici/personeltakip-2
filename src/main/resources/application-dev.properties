# Development Environment Configuration

# Database - Local
spring.datasource.url=jdbc:mysql://localhost:3306/personnel_tracking?useSSL=false&serverTimezone=Europe/Istanbul&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=sahinbey_
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Database - External
external.datasource.url=jdbc:mysql://193.140.136.145:3306/isicil?useSSL=false&serverTimezone=Europe/Istanbul&useUnicode=true&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
external.datasource.username=${EXTERNAL_DB_USERNAME:hesapi}
external.datasource.password=${EXTERNAL_DB_PASSWORD:TuruncuManTiS2025!!}
external.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
external.datasource.hikari.connection-test-query=SELECT 1
external.datasource.hikari.leak-detection-threshold=60000

# External JPA/Hibernate Configuration
external.jpa.hibernate.ddl-auto=none
external.datasource.hikari.auto-commit=true
external.datasource.hikari.read-only=true

# JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# JWT (use weak secret for dev only - NEVER use in production)
jwt.secret=dev-secret-key-not-for-production-use-only
jwt.expiration=1800000
jwt.issuer=personnel-tracking-system-dev

# SMS Gateway - Mock SMS for testing (shows OTP in console)
mock.sms.enabled=true
sms.gateway.url=http://localhost:8080/mock-sms
sms.gateway.api-id=29d463733f56db81be9eb355
sms.gateway.api-key=79259ea325e14e8603ab7cf7
sms.gateway.sender=G.ANTEP UNI
otp.length=6

# Logging
logging.level.com.bidb.personetakip=DEBUG
logging.level.org.springframework.security=DEBUG

# Disable SSL in development
server.ssl.enabled=false

# CORS - Allow all origins in development
cors.allowed-origins=*
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS,PATCH
cors.allowed-headers=*
cors.allow-credentials=true
cors.max-age=3600

# Thymeleaf hot reload
spring.thymeleaf.cache=false

# IP Tracking - Development Settings
ip.tracking.enabled=true
ip.tracking.privacy.enabled=false
ip.tracking.privacy.anonymize-reports=false
ip.tracking.privacy.audit-logging=true
ip.tracking.privacy.retention-days=30
ip.tracking.privacy.require-consent=false
ip.tracking.anonymization.enabled=false
ip.tracking.performance.capture-timeout-ms=2000
ip.tracking.performance.async-logging=false
ip.tracking.performance.cache-size=100
# OpenAPI/Swagger UI Configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.try-it-out-enabled=true
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.tags-sorter=alpha
springdoc.swagger-ui.display-request-duration=true
springdoc.swagger-ui.doc-expansion=none

# Application Version
app.version=1.0.0

# QR Code Configuration - Development Settings
qr.code.max-usage-per-day=999
qr.code.development-mode=true

# Timezone Configuration
spring.jpa.properties.hibernate.jdbc.time_zone=Europe/Istanbul
server.servlet.session.timeout=30m

# Jackson JSON serialization timezone
spring.jackson.time-zone=Europe/Istanbul
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss